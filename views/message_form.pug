extends layout 

block content 
    h1 #{title}

    form(method="POST" action="")
        div
            label(for="message") Write a message:
            div
            textarea#message(type="text" name="message" rows="5" cols="50")
        button(type="submit") Submit

    if errors 
        ul
            for error in errors
                li!= error.msg